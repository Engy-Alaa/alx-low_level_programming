#include "main.h"
/**
 * main -  test that the function positive_or_negative print
 * correct output
 * Return: no return
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
