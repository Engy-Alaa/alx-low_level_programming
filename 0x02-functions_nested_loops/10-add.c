#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @n:first integer
 * @m:second integer
 * Return: returns result
 */
int add(int n, int m)
{
	int result = n + m;

	return (result);
}
